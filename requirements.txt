# AI Fencing Coach & Referee System - Requirements

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Computer Vision
opencv-python>=4.8.0
ultralytics>=8.0.0

lap>=0.5.12  # Optional for Ultralytics ByteTrack persistent IDs; runtime falls back without it
numpy>=1.24.0,<2.0.0  # Constraint added to prevent pip backtracking on numpy 2.0

# Deep Learning & NLP
transformers>=4.30.0
Pillow>=9.5.0

# Model quantization (optional but recommended)
bitsandbytes>=0.40.0

# Data handling
pandas>=2.0.0
scipy>=1.10.0
gradio>=4.0.0

# LLM / Gemini (use google-generativeai instead of google-genai to avoid websockets conflict with gradio)
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Real-time voice coaching (offline TTS)
pyttsx3>=2.90

# Utilities
pyyaml>=6.0.0
tqdm>=4.65.0

# Logging & monitoring
tensorboard>=2.13.0

# Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.4.0

# Jupyter support (for notebooks)
jupyter>=1.0.0
ipykernel>=6.21.0
ipywidgets>=8.0.0
