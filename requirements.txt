# AI Fencing Coach & Referee System - Requirements
#
# Use Python 3.11 or 3.12 on Windows. Python 3.14 is too new for this stack:
# numpy<2.0, PyTorch, and ONNX tooling may not have compatible wheels, causing
# pip to try slow source builds that require Visual Studio C++ compilers.

# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Computer Vision
opencv-python>=4.8.0
ultralytics>=8.0.0

lap>=0.5.12  # Optional for Ultralytics ByteTrack persistent IDs; runtime falls back without it
numpy>=1.24.0,<2.0.0  # Constraint added to prevent pip backtracking on numpy 2.0

# Deep Learning & NLP
transformers>=4.30.0
Pillow>=9.5.0

# Model quantization (optional but recommended)
bitsandbytes>=0.40.0
onnx>=1.16.0
onnxruntime>=1.18.0
onnxscript>=0.1.0

# Data handling
pandas>=2.0.0
scipy>=1.10.0
gradio>=4.0.0

# LLM / Gemini (use google-generativeai instead of google-genai to avoid websockets conflict with gradio)
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Real-time voice coaching (offline TTS)
pyttsx3>=2.90

# Utilities
pyyaml>=6.0.0
tqdm>=4.65.0

# Logging & monitoring
tensorboard>=2.13.0

# Development
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.4.0

# Jupyter support (for notebooks)
jupyter>=1.0.0
ipykernel>=6.21.0
ipywidgets>=8.0.0
